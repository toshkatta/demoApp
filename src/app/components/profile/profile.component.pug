h2.text-center Profile

.container
    .row
        .col.col-lg-3
            h5 Username
        .col.col-lg-9
            h5 {{user.name}}
    .row
        .col.col-lg-3
            h5 Email
        .col.col-lg-9
            h5 {{user.email}}
    .row
        .col.col-lg-3
            h5 Id
        .col.col-lg-9
            h5 {{user.id}}